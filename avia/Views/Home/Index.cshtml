@model HomeModel
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>@if (Model.HelloMessage != null)
        { @Model.HelloMessage; }

    </p>
</div>
